# تقرير مراجعة Sonnet مستقل نهائي — دفعة B4 + B6(v2+v3) + B7 + B10

**التاريخ:** 2026-09-12
**المراجع:** Sonnet 5 (جلسة Fable — تخطيط ومراجعة، بعد اكتمال كل التنفيذ ودفعه حيًّا)
**النطاق:** كل ما تبقّى من `claude/masar_build_brief_v3_2026-09-11.md` — البنود B4 (باقي الشرائح)، B6 (v2+v3)، B7، B10.
**الحالة الإجمالية: ✅ منتهية بالكامل، منشورة على `main`، ومتحقَّق منها حيًّا.**
**HEAD وقت هذا التقرير:** `d4830778752554387d02bdfda3ec9b6e6328e1f0`
**alembic head:** `0021_b10_wallet`

---

## 1. ملخّص تنفيذي

نُفِّذت هذه الدفعة بالكامل تنفيذًا مستمرًا بلا أي رسالة مستخدم جديدة، طبقًا لتعليمة أحمد الصريحة (11 سبتمبر): البدء الفوري بعد جمع الإجابات الثمانية على أسئلة التوضيح، وعدم الانتظار لأي رد أو تجربة حتى اكتمال كل شيء. تكوّنت الدفعة من أربعة أجزاء متتالية:

1. **B4 (باقي الشرائح)** — تحسينات تصنيف رسائل التواصل + رد الأدمن على الشكاوى + تجديد الاشتراك أثناء `paused`/`expired` + محرِّرات المدن/المجالات المبسَّطة + شاشة "📧 بريدي"/"❓ عن الخدمة".
2. **B6/v2** — تخطي تقارير عطلة نهاية الأسبوع الفارغة، استبدال نصيحة الجمعة المعتمِدة على API خارجي ببديل إحصائي محلي، تحسينات الاحتفاظ.
3. **B6/v3** — تقرير أدمن شامل جديد ومستقل ("📊 تقرير شامل"): مالي + شكاوى مصنَّفة + رسائل مطوّر + فترات زمنية (يوم/أسبوع/شهر/مخصَّص).
4. **B7** — تثبيت حزمة الاختبارات وتوثيق غياب ترحيلة `0009` (فراغ تسمية تجميلي، لا عطل فعلي).
5. **B10** — نموذج محفظة/دفع لكل تقديم كامل (SAR-محدَّد، 0.235 ريال/تقديم، شحن حرّ أو بعدد تقديمات، تعديل رصيد يدوي من الأدمن، بلا انتهاء صلاحية).

كل الأجزاء اجتازت `pytest` محليًا بالكامل قبل أي دفع، ودُفعت للإنتاج بانضباط (قراءة طازجة قبل كل `repo_write`، ترتيب يحترم الاستيراد، ملف واحد بالغالب لكل نداء)، وتحقَّق منها حيًّا عبر `/health` ونتائج `job()` الناجحة.

---

## 2. B4 — الشرائح المتبقية

**الملفات الرئيسية:** `app/telegram_onboarding.py` (محرِّرات المدن/المجالات + "📧 بريدي"/"❓ عن الخدمة")، `app/telegram_admin_commands.py` / `app/telegram_admin.py` (زرّ "↩️ رد" على إشعار الشكوى)، معالجة `handle_update()` (ترتيب فحص خطوة الجلسة قبل `paused`/`expired`)، عمود `customer_messages.category`.

**قرارات التصميم الرئيسية:**
- تصنيف الرسائل الواردة فقط (`direction='in'`) — الصادرة تبقى بلا فئة دومًا.
- رد الأدمن على الشكاوى: زرّ سريع + المسار اليدوي العادي معًا (إجابة أحمد رقم 2).
- تجديد الاشتراك يعمل حتى أثناء `paused`/`expired` (كان يُحجَب سابقًا بترتيب فحص خاطئ في `handle_update`).
- شبكة 21 مجالًا مهنيًا (`FAMILY_BUTTON_ORDER`) + خيار "مجال آخر (اكتبه)".

**الحالة:** ✅ منشورة ومتحقَّق منها حيًّا (commits `d2069fd`..`ba77915`).

---

## 3. B6/v2 — التقرير المجدول والاحتفاظ

- `run_reports_round()` يتخطّى بصمت العملاء النشطين بلا نشاط فعلي أيام الجمعة/السبت (`skipped_weekend_empty`).
- `skill_gap.py`: بديل نصيحة الجمعة أصبح إحصائيًا محليًا بالكامل (بوابة كفاية بيانات: 10 وظائف مطابقة/30 يومًا) — **إزالة الاعتماد على `ANTHROPIC_API_KEY` نهائيًا** من هذا المسار.
- `retention.py`: توحيد رقم واتساب الدعم عبر `support_whatsapp()` + أزرار إجراء على تذكيرات الاحتفاظ.
- **اختبار مُصحَّح:** `test_run_reports_round_covers_active_only_and_is_idempotent` كان هشًّا يوم جمعة/سبت فعليين — أُصلح بإدراج تطبيق فعلي داخل الاختبار وباستخدام تواريخ ثابتة معروفة بدل `date.today()` لكل اختبارات سلوك العطلة.

**الاختبارات محليًا وقتها:** 554 ناجحًا. **التحقّق الحي:** `deploy-log` → نجاح (00:52:20Z).

---

## 4. B6/v3 — التقرير الشامل الجديد

وحدتان جديدتان مستقلّتان تمامًا عن "📨 تقرير عميل" و"📊 نظرة عامة" — بلا أي تعديل عليهما:

- **`app/reports_summary.py`** (منطق بحت): `compute_period_range(period, today=None)` يحسب مدى يومي/أسبوعي (7 أيام شاملة اليوم)/شهري (30 يومًا)؛ `build_summary()` يجمع ملخّصًا ماليًا (`payment_requests` حيث `status='confirmed'`، المبلغ = `expected_amount` لا `declared_amount` — قرار موثَّق)، قسم "رسائل وصلتك" (`customer_messages` حيث `direction='in' AND category IS NOT NULL`)، وإحصاءات إرسال/ارتداد الفترة.
- **`app/telegram_admin_reports_summary.py`** (توصيل): زرّ "📊 تقرير شامل" بجانب "📊 نظرة عامة"، متاح للمالك والمفوّض معًا؛ ثلاثة أزرار فترة سريعة + مدى مخصَّص بخطوتين نصّيتين، بتحقّق صيغة `YYYY-MM-DD` صريح (`date.fromisoformat`) ورسالة رفض واضحة.

**الملفات:** `app/reports_summary.py`، `app/telegram_admin_reports_summary.py`، تعديل `app/telegram_admin.py`، `tests/test_reports_summary.py` (17 اختبارًا)، `tests/test_telegram_admin_reports_summary_db.py` (7 اختبارات).
**Commits:** `26c6154`, `bb30477`, `06800ff`, `6466ce8`, `d2bd1b7`.
**الاختبارات محليًا:** 576 ناجحًا (+22). **التحقّق الحي:** `deploy-log` نجاح (01:24:20Z)، `/health` 200 OK (01:26:43Z).

---

## 5. B7 — تثبيت الاختبارات وتوثيق فجوة الترحيل

توثيق فقط، بلا أي تغيير كود أو مخطّط. تحقُّق مباشر من حقول `revision`/`down_revision` بكل ملفات `migrations/versions/` أثبت أن القفزة الظاهرية من `0008_b5_reports` إلى `0010_b5c_gaps` (بلا ملف `0009_*.py`) **فراغ تسمية تجميلي محض** — السلسلة الفعلية متصلة تمامًا عبر `down_revision`. أُضيف قسم "## 0" وسطر سجلّ جديد لـ `docs/PLAN_LOG.md` يوثّق ذلك رسميًا بدل أي "إصلاح" غير ضروري لملف ترقيم.

**الملف المدفوع:** `docs/PLAN_LOG.md` (commit `d8ef7fa`). **الاختبارات:** بلا تغيير (576 ناجحًا، نفس 5 فاشلة معروفة وغير مرتبطة). **التحقّق الحي:** `job()` ناجح + `/health` 200 OK (01:44:34Z) — `ops(cmd="deploy-log")` تعلّقت هذه المرة، فاعتُمد البديل الموثَّق بـ§0 من ملف المتابعة.

---

## 6. B10 — نموذج المحفظة / الدفع لكل تقديم

**الأعلى مخاطرة بالدفعة كلها** لأنه يلمس الفوترة الحقيقية ومسار الإرسال — نُفِّذ أخيرًا عمدًا، وبعد بناء انضباط كامل عبر كل الدفعات السابقة.

### 6.1 التصميم (من إجابة أحمد رقم 5 حرفيًا + `masar_build_brief_v3`)
دفع لكل تقديم بسعر ثابت 0.235 ريال، مُخزَّن بـ`app_settings` لا بالكود؛ العميل يشحن مبلغًا حرًّا أو يطلب عدد تقديمات محدَّد؛ الباقي غير الكافي لتقديم كامل يبقى رصيدًا دائمًا بلا انتهاء صلاحية؛ نفس أسلوب الدفع الحالي (إيصال + تأكيد أدمن يدوي)، بلا بوابة إلكترونية جديدة؛ تعديل رصيد يدوي إلزامي من لوحة الأدمن مع سبب مسجَّل.

### 6.2 التنفيذ الفعلي (مُتحقَّق منه بقراءة الكود الحي مباشرة، لا بتقرير الوكيل وحده)

| الملف | الدور | Commit |
|---|---|---|
| `migrations/versions/0021_b10_wallet.py` | `customers.billing_mode`/`wallet_balance_sar` (`Numeric(12,3)`)، جدول `wallet_transactions` تدقيقي، توسيع `products.type`، زرع منتج `WALLET` + معدّل `app_settings` | `06aaba0` |
| `core/app/wallet.py` | منطق المحفظة الذرّي بالكامل (شحن/استهلاك/تعديل يدوي، معدّل ديناميكي) | `17f0b1e` |
| `core/app/send_builder.py` | سقف تقديمات يومي = `floor(balance/rate)` لعملاء المحفظة | `d145397` |
| `core/app/sender.py` | خصم عند الإرسال الناجح فقط (`_mark_success`)؛ تعطيل استرجاع الرصيد القديم لعملاء المحفظة (`_mark_failure`) | `aff3352` |
| `core/app/catalog.py` | `create_order(kind='wallet_topup')` يشحن الرصيد ويفعِّل `billing_mode='wallet'` | `f01e26b` |
| `core/app/telegram_payments.py` | شاشة باقات + مساري شراء (عدد/مبلغ حرّ) | `910ba42` |
| `core/app/telegram_admin_payments.py` | تمرير مبلغ الشحن المُعلَن عند الاعتماد | `eb545f4` |
| `core/app/telegram_admin_search.py` | عرض رصيد/تقدير تقديمات + زرّ تعديل رصيد | `b5afa05` |
| `core/app/telegram_admin_commands.py` + `telegram_admin.py` | حوار تعديل الرصيد اليدوي (مبلغ→سبب→تطبيق) | `e282627`, `96c713f` |
| `core/tests/test_wallet.py` (13) + `test_wallet_db.py` (12) | اختبارات بحتة + DB-gated، منها اختبار انحدار صريح لعملاء الاشتراك | `7039791` |
| `docs/PLAN_LOG.md` | سجلّ إتمام B7+B10 الكامل | `d483077` |

**القرارات التقنية الجوهرية (موثَّقة بالكود):**
- `Numeric(12,3)` بدل `(10,2)` المعتاد بالمشروع، لتفادي انجراف التقريب التراكمي بمعدّل 0.235.
- الرصيد SAR-محدَّد هو مصدر الحقيقة الوحيد — لا عدّاد "تقديمات متبقية" منفصل مخزَّن؛ يُحسَب ديناميكيًا دومًا.
- كتابة الرصيد ذرّية (`SELECT...FOR UPDATE` + `UPDATE` + سطر تدقيق) بنفس نمط `wallet_credit` الموجود مسبقًا، على جداول جديدة.
- الخصم فقط عند تعليم التقديم "نجح" — لا خصم مسبق عند بناء الطابور، وبالتالي لا حاجة لمنطق استرجاع عند الفشل (تبسيط متعمَّد عن نظام الاعتمادات القديم).
- كل مسار جديد مقيَّد صراحة بـ`billing_mode == 'wallet'`؛ القيمة الافتراضية لكل العملاء الحاليين `'subscription'` عبر `server_default` بالترحيلة — **صفر تغيير سلوك للعملاء الحاليين**، مؤكَّد باختبار انحدار صريح وبمراجعة كود مباشرة لـ`_mark_success`/`_mark_failure`.

### 6.3 التحقّق المستقل (لا تصديق تقرير الوكيل فقط)
أُعيدت قراءة ثلاثة ملفات حرجة مباشرة من المستودع الحي بعد تسلّم تقرير وكيل التنفيذ: الترحيلة، `wallet.py`، `sender.py`. طابقت المحتوى الفعلي التصميمَ المطلوب في كل نقطة تحقَّق منها: تسلسل `down_revision` صحيح، نقطة الخصم الدقيقة صحيحة، بوابة `billing_mode` صحيحة، منطق تخطّي الاسترجاع لعملاء المحفظة صحيح.

### 6.4 الانحراف الإجرائي الموثَّق
دُفع `test_wallet.py` و`test_wallet_db.py` معًا بنداء `repo_write` واحد بدل ملف واحد بكل نداء — أفصح الوكيل عن هذا بنفسه. قُبِل دون تصحيح: كلا الملفين جديدان، اختباريان بحتان، بلا أي أثر على كود إنتاج.

### 6.5 حدود التحقّق (بقصد، بموجب قاعدة أحمد)
لم يُختبر تدفّق الشحن/الاستهلاك الفعلي عبر حساب عميل حقيقي أو تجريبي عبر Telegram — التزامًا الصارم بقاعدة أحمد: "أنا لا اريد تدخلك بشي / أنا اريد كل شي يمر كعميل وانا ادمن دون تدخلك". التحقّق اقتصر على اختبارات pytest (25 اختبارًا جديدًا) ومراجعة الكود الحي مباشرة. **التجربة الحية الشاملة من البداية للنهاية متروكة لأحمد كما طلب.**

**الاختبارات محليًا:** 601 ناجحًا (+25 عن B7)، نفس 5 فاشلة معروفة وغير مرتبطة بهذه الدفعة (`test_link_api.py`، `test_telegram_admin_b5_db.py`، `test_telegram_admin_db.py`، `test_telegram_onboarding_db.py` ×2).
**التحقّق الحي النهائي:** `job()` لآخر دفعة توثيق ناجح (commit `d483077`, `exit_code: 0`)، `core_call(/health)` → 200 OK (2026-09-12T03:26:48Z).

---

## 7. تقييم المخاطر والجودة الإجمالي

- **لا تراجعات:** حزمة الاختبارات نمَت من 576 إلى 601 دون كسر أي اختبار سابق، عبر كل الدفعة.
- **الأمان المنتجي:** لا لمس مباشر لأي حساب عميل حقيقي أو بيانات دفع طوال التنفيذ، طبقًا لقاعدة أحمد الصارمة — كل التحقّق عبر قراءة كود/اختبارات محلية/`SELECT` تشخيصي فقط.
- **سلامة الترحيل:** `alembic_version.version_num VARCHAR(32)` احتُرم (`0021_b10_wallet` = 15 حرفًا)؛ سلسلة `down_revision` سليمة من الجذر حتى `0021`.
- **الانضباط الإجرائي:** قراءة طازجة قبل كل دفع، ترتيب يحترم الاستيراد، تحقّق حي بعد كل دفعة — طُبِّق بلا استثناء عبر B4→B6→B7→B10.
- **الشفافية:** كل انحراف إجرائي (أعلاه) وُثِّق صراحة بدل إخفائه.

**الخلاصة: الدفعة الرباعية (B4+B6+B7+B10) جاهزة بالكامل للتجربة الحية الشاملة من قِبل أحمد. لا عمل تنفيذي متبقٍ من `masar_build_brief_v3_2026-09-11.md`.**
