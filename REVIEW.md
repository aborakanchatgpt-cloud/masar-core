# REVIEW.md — تصحيحات المراجع المفتوحة (يكتبه المراجع فقط؛ المنفّذ يقرأه ويطبّقه)

> الصيغة: `R<n> — OPEN|APPLIED — <البند> — <التصحيح بدقة: الملف، ماذا يتغيّر، ولماذا>`
> المنفّذ يطبّق كل `OPEN` قبل بدء أي بند جديد ويكتب في PLAN.md §3: `applied R<n>`. المراجع يحوّلها إلى `APPLIED` بعد التحقق.

(لا تصحيحات مفتوحة)
