fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
psycopg[binary]==3.2.1
python-dotenv==1.0.1
apscheduler==3.10.4
httpx==0.27.2
sqlalchemy==2.0.35
alembic==1.13.3
pyyaml==6.0.2
cryptography==43.0.1
jinja2==3.1.4
python-multipart==0.0.9
# B8: أول قدرة استخراج نص PDF بالمستودع — تحتاجها app.telegram_onboarding
# لتحويل ملف السيرة الذاتية المرفوع عبر تيليجرام لنص خام قبل تمريره لمسار
# الاستخراج الحتمي الموجود أصلًا (app.customers_api.upsert_profile).
# pure-Python بالكامل (لا اعتماد نظام تشغيل خارجي كـpoppler) — يطابق فلسفة
# "تبعية خفيفة" المطلوبة لطبقة تيليجرام.
pypdf==4.3.1
# B12.1: مجمّع قنوات تيليجرام للوظائف (core/app/collectors/telegram_channels.py)
# — عميل MTProto (لا Bot API) لقراءة رسائل قنوات عامة بحساب قراءة مخصّص
# (TELEGRAM_READER_* env، راجع docstring الوحدة). الاستيراد داخل try/except
# بكل مكان يُستخدم فيه — غياب الحزمة لا يجب أن يُسقط core-scheduler أبدًا.
telethon==1.36.0
