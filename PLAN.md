# خطة المرحلة 1 — سجل تقدّم حي

آخر تحديث: 5 سبتمبر 2026 (بدأ التنفيذ بناءً على طلب أحمد التنفيذ الفوري والمتواصل).

## الحالة العامة: كل ما لا يحتاج أحمد شخصيًا اكتمل (المرحلة 1 كاملة + بداية المرحلة 2) — متوقفة عند نفس الخطوتين فقط

لا يوجد أي شيء بالمرحلة 1 يحتاج قرارًا من أحمد — كل التفاصيل التقنية محسومة بالدليل
ونُفّذت مباشرة. لكن **إنشاء حسابين مدفوعين (GitHub لو ما عنده، وHetzner Cloud) هو
إجراء لا يُسمح لي بتنفيذه تلقائيًا مهما كانت الموافقة المسبقة** — سياسة صارمة تمنعي
من إنشاء حسابات أو إدخال بيانات دفع نيابة عن أي شخص، بدون استثناء. هذا ليس قرارًا
بالدليل يحتاج تفضيلًا، بل قيد تقني/سياسي عندي يستوجب إخبار أحمد فورًا (تمامًا مثل
نقص صلاحية لا أقدر أتجاوزها بنفسي).

## ما تم إنجازه فعليًا (بدون انتظار)

- [x] هيكل مستودع `masar-core` كامل جاهز بمساحة العمل السحابية:
  - `core/app/main.py` — خدمة FastAPI بنقطة `/health` (ونقطة `/`)
  - `core/Dockerfile`, `core/requirements.txt`
  - `docker-compose.yml` — Postgres 16 + Gotenberg 8 + Core + Caddy (HTTPS تلقائي عبر دومين)
  - `Caddyfile`, `.env.example`, `.gitignore`
  - `deploy/bootstrap.sh` — يجهّز خادم Ubuntu 24.04 جديد بالكامل تلقائيًا (Docker، جدار حماية UFW، استنساخ المستودع، تشغيل الحزمة) — سطر تشغيل واحد فقط
  - `deploy/autodeploy.sh` — سحب أي commit جديد وإعادة النشر تلقائيًا (مصمم للتشغيل كل دقيقة عبر cron، يحقق معيار "خلال 5 دقائق" بهامش كبير)
  - `deploy/backup.sh` — نسخة احتياطية يومية مشفّرة (GPG/AES256) من قاعدة البيانات
  - `README.md`

## قيد تقني إضافي مكتشف (5 سبتمبر، بعد طلب أحمد استخدام device_bash)

جرّبت تنفيذ الخطوات عبر SSH كما طلب أحمد، واكتشفت قيدين تقنيين حقيقيين (وليس
قرار سياسة فقط):
1. **`device_bash` غير متاح بهذه الجلسة** — الجهاز المرتبط (Windows) يعطي فقط
   أداة `get_device_info` (لا shell فعلي على جهاز أحمد من هذه الجلسة).
2. **لا يوجد اتصال SSH خام (منفذ 22) من بيئة العمل السحابية الخاصة بي** — فحصت
   ذلك فعليًا (SSH إلى github.com على المنفذ 22 وعلى المنفذ 443 كلاهما فشل؛
   فقط HTTPS يعمل عبر البروكسي المُعدّ مسبقًا).

النتيجة: لا أقدر تنفيذ أي أمر SSH من أي جهة (لا من جهاز أحمد ولا من بيئتي)، بغض
النظر عن سياسة الحسابات/الدفع. الخطوات التالية تحتاج تنفيذ أحمد بنفسه، لكنها
قصيرة جدًا (نسخ ولصق، أقل من 5 دقائق إجمالًا) ولا تحتاج أي قرار تقني منه.

## ما يحتاج أحمد شخصيًا (أربع خطوات قصيرة، نسخ-لصق فقط)

### 1) رفع الكود لـ GitHub (من جهازه)
حمّل `masar-core.zip` المُرسل بالمحادثة، فك الضغط، ثم من الطرفية بجهازه (بعد
إنشاء مستودع خاص فارغ باسم `masar-core` على حسابه):
```bash
cd masar-core
git init -b main
git remote add origin https://github.com/<GITHUB_USER>/masar-core.git
git add .
git commit -m "Initial masar-core scaffold"
git push -u origin main
```
يستخدم مصادقة GitHub الموجودة أصلًا بجهازه (git credential manager أو gh cli) —
لا حاجة لتوكن جديد لهذه الخطوة.

### 2) Hetzner Cloud — خادم CX22
أنشئ حساب (يحتاج بطاقة دفع)، Project باسم `masar`، ثم خادم: Ubuntu 24.04، CX22،
فعّل Backups، وأضف مفتاح SSH (لو ما عندك: `ssh-keygen -t ed25519 -C "masar-core"`
من طرفية جهازه، ثم الصق محتوى `~/.ssh/id_ed25519.pub` بخانة SSH Key).

### 3) توكن GitHub للخادم (يُستخدم على جهازه/خادمه فقط، لا يُشارك بالمحادثة)
Settings → Developer settings → Fine-grained tokens → توكن جديد لمستودع
`masar-core` فقط بصلاحيات Contents: Read/Write.

### 4) تشغيل bootstrap (SSH واحد من جهازه)
```bash
ssh root@<IP_الخادم>
git clone https://<TOKEN>@github.com/<GITHUB_USER>/masar-core.git /opt/masar-core
cd /opt/masar-core
sudo bash deploy/bootstrap.sh
```
السكربت يتولى الباقي تلقائيًا. آخر خطوة يدوية: عدّل `MASAR_DOMAIN` بملف `.env`
على الخادم (بعد ما يشير DNS الدومين لعنوان IP الخادم).

### UptimeRobot
حساب مجاني بالبريد، مراقب HTTPS يشير لـ `https://<MASAR_DOMAIN>/health`.

## بمجرد توفر الدومين الفعلي (يعمل عبر HTTPS)

أكمل مباشرة وبدون انتظار (هذا الجزء ضمن قدرتي فعليًا، HTTPS يعمل من بيئتي): التحقق
من `/health`، تعديل حقل `baseUrl` بوركفلو Core - Call، اختبار الوركفلوهات الثلاثة
فعليًا، تحديث `PLAN.md`، ثم المتابعة المباشرة للمرحلة 2 الميدانية (اعتماد الشركات
الفعلية وتشغيل الجامع الحقيقي).

## وركفلوهات الجسر (n8n) — تم إنشاؤها فعليًا (بانتظار ربط الدومين فقط)

- [x] **Core - Call** (`v7xKPShYWHJwqxvs`) — Webhook → Set (`baseUrl` placeholder قابل للتعديل بحقل واحد) → HTTP Request (يمرّر method/path/body) → Respond to Webhook. بعد نشر الخادم: افتح الوركفلو وعدّل قيمة `baseUrl` بعقدة "Core Config" من `https://REPLACE_WITH_YOUR_DOMAIN` إلى الدومين الفعلي، هذا كل المطلوب.
- [x] **Core - Notify Admin** (`UubD6Kba97XGJlVt`) — Webhook → Execute Sub-workflow (يستدعي بوت الأدمن الحالي `ZiVlsBibVps3pxGB` مباشرة بدون تكرار منطق) → Respond to Webhook.
- [x] **Core - Notify Customer** (`S6AuI9VPaaQxhfhw`) — Webhook → Telegram (نفس اعتماد "Telegram account" الحالي، id `3LNL2j1yLucpqw26`) → Respond to Webhook.

الثلاثة جاهزة تقنيًا الآن ويمكن اختبارها فورًا (حتى بدون خادم Masar Core حقيقي، فقط Core - Call سيفشل باتصال HTTP حتى يتوفر baseUrl حقيقي). لا حاجة لأي إجراء إضافي من أحمد هنا سوى تعديل حقل baseUrl لاحقًا.

## معيار قبول المرحلة 1 — لم يتحقق بعد (يحتاج الخادم الفعلي)

كل ما هو قابل للبناء دون خادم فعلي أو حساب GitHub تم إنجازه. لكن معيار القبول
نفسه (`/health` يعمل عبر HTTPS، تحديث GitHub يظهر خلال 5 دقائق، Gotenberg
يحوّل HTML تجريبيًا، n8n يستدعي Core بنجاح) **لا يمكن التحقق منه فعليًا** قبل
توفر الخادم — هذا التحقق أول شيء يُنفَّذ بمجرد توفر رابط GitHub وIP الخادم.

## بداية المرحلة 2 (اكتشاف ومعجم) — أُنجز الجزء الذي لا يحتاج خادمًا

بما أن قسم 10 بالدليل ينص على المتابعة دون انتظار في كل ما لا يحتاج قرارًا أو
دفعًا من أحمد، وبما أن معيار قبول المرحلة 1 نفسه معلّق على نفس العائقين
المذكورين أعلاه (GitHub + Hetzner)، بدأت ببناء الأجزاء من المرحلة 2 القابلة
للتنفيذ والاختبار محليًا بدون خادم فعلي:

- [x] `data/taxonomy_local.yaml` — مسودة أولى لكل العائلات الـ12 المطلوبة (عربي
  + إنجليزي)، بُنيت على مجال أحمد (الهندسة الكيميائية) والعائلات المذكورة
  بالدليل. تحتاج مراجعة وتوسيع من Source Curator لاحقًا كما ينص الدليل.
- [x] `migrations/` (Alembic) — جداول: `companies`, `sources`, `taxonomy_terms`,
  `jobs` (بمفتاح تكرار `dedup_key` فريد)، `metrics_hourly`. جاهزة للتشغيل
  (`alembic upgrade head`) بمجرد توفر Postgres حقيقي.
- [x] `core/app/collectors/` — **8 جامعات** كاملة، كل واحد مُختبر (mock اختبار
  وحدة يطابق الشكل الحقيقي المؤكّد من التوثيق الرسمي أو بفحص مباشر فعلي):
  - `greenhouse.py`, `lever.py`, `ashby.py` (تحقّق مباشر فعلي عبر WebFetch —
    استعلام حي ناجح)، `smartrecruiters.py`, `workable.py`, `recruitee.py` —
    ستة أنظمة ATS بواجهات JSON عامة موثّقة بدون مصادقة.
  - `sitemap_jsonld.py` — جامع عام (fallback) يستخرج بيانات schema.org
    JobPosting المُضمّنة كـ JSON-LD من أي صفحة وظائف — هذا هو الحل الصحيح
    حسب الدليل نفسه للشركات التي تستضيف على Teamtailor/BambooHR/Workday أو
    صفحة مخصّصة، بما أن هذه الأنظمة **لا توفّر واجهة JSON عامة موثّقة بدون
    مصادقة** (تحقّقت من هذا بالبحث: Teamtailor يتطلب توكن API رسمي،
    BambooHR ليس له REST endpoint موثّق للوظائف) — فبناء جامع مخصص "يعمل"
    لهذه الأنظمة كان سيكون بناءً على تخمين غير موثوق، فاخترت الحل الصحيح
    والصادق بدلًا من ذلك.
  - `rss.py` — خلاصات RSS/Atom عامة (بدون مكتبات خارجية).
  - `alert_mail.py` — **على الأرجح الأهم فعليًا للسوق السعودي/الخليجي**:
    بحثت فعليًا (WebSearch) عن شركات كيميائية/معالجة مياه/بتروكيماويات
    سعودية على Greenhouse/Lever/Ashby ولم أجد سوى نتيجة حقيقية واحدة قوية
    (انظر أدناه) — أغلب أصحاب العمل الكبار بالسوق المحلي (أرامكو، سابك،
    شركات التحلية) يستخدمون بوابات توظيف مخصصة أو ينشرون عبر Bayt/
    GulfTalent/LinkedIn/Indeed، وليس أنظمة ATS الحديثة ذات API عام. الحل
    العملي الموثوق: قراءة تنبيهات البريد الرسمية من هذه اللوحات عبر IMAP
    (بُني كإطار عمل جاهز + أنماط استخراج روابط لكل مزوّد، بانتظار معايرة
    دقيقة على عينات حقيقية بمجرد ربط صندوق بريد فعلي).
- [x] `core/app/collectors/field_extractor.py` — المطبّع الكامل المطلوب:
  سنوات الخبرة (نطاق أو "+")، مستوى الأقدمية، شرط الجنسية السعودية الصريح،
  مدن معروفة (سعودية/خليجية)، مهارات معروفة (SAP, Aspen HYSYS, Six Sigma...)،
  تصنيف نوع التقديم (نموذج خارجي/بريد). كل دالة مُختبرة بأمثلة عربي+إنجليزي.
- [x] `scripts/import_esco.py` — استيراد ESCO لجدول `taxonomy_terms`: يقرأ
  ملف CSV محلي (يُحمَّل يدويًا من esco.ec.europa.eu على الخادم — لا يحتاج
  اتصال شبكة وقت التشغيل)، يقارن كل preferredLabel/altLabel بكلمات
  taxonomy_local.yaml، ويستورد المطابقات فقط (مع وضع dry-run بدون
  DATABASE_URL). مُختبر بملف CSV اصطناعي مطابق لشكل ESCO الحقيقي.
- [x] `data/companies_seed.csv` — بدأت التحقق الفعلي (WebSearch + WebFetch،
  وليس تخمينًا): وجدت شركة واحدة حقيقية ومؤكدة 100%: **Zeeco** (شركة معدات
  احتراق/حرق للنفط والغاز، لوحة Lever فعالة بها عشرات الوظائف الحالية
  بمسمى Process/Quality/HSE Engineer بمواقع Dammam وYanbu — تطابق مباشر
  لمجال أحمد). محاولات إضافية لشركات كيميائية/مياه سعودية أخرى على
  Greenhouse/Lever/Ashby لم تُثمر (نتائج البحث تؤكد أن هذه الشركات ليست
  على هذه الأنظمة). **لم أُكمل لـ60 شركة** لأن ذلك يحتاج بحثًا مكثفًا
  حقيقيًا لكل شركة (تمامًا كما يطلب الدليل: "التحقق يدويًا من كل نقطة") —
  وهذا بالضبط ما يصفه الدليل نفسه كجلسة "Source Curator" **أسبوعية متكررة**
  وليس مهمة تُنجز دفعة واحدة؛ سأكمل هذا تدريجيًا بجلسات قادمة بدل تلفيق
  قائمة غير محقّقة فعليًا (يخالف مبدأ الدليل: "اختبر بالأرقام لا بالانطباع").
- [x] `core/app/scheduler_main.py` + خدمة `core-scheduler` بـ docker-compose —
  هيكل تشغيل الجامع كل 30 دقيقة (APScheduler)؛ الربط الفعلي بجدول `sources`
  النشطة يُستكمل بعد اعتماد دفعة شركات كافية.
- [x] تحقّق محلي شامل: تثبيت كل المتطلبات بنجاح، اختبار وحدة (unit test) لكل
  جامع بأمثلة تطابق الشكل الحقيقي المؤكد، فحص YAML/compose، `docker compose
  config` ناجح بالكامل.

**ما تبقّى من المرحلة 2 ويحتاج فعليًا خادمًا حيًا وعملًا ميدانيًا متواصلًا:**
استكمال Source Curator لعدد أكبر من الشركات (بحث حقيقي مستمر، ليس دفعة واحدة)،
تشغيل الجامع فعليًا وقياس `metrics_hourly`، تحميل ملف ESCO الفعلي وتشغيل
`import_esco.py` عليه، ربط صندوق بريد فعلي ومعايرة `alert_mail.py` على عينات
حقيقية، استيراد الوظائف/الشركات الحالية من Data Tables كبذرة. هذه الخطوات
تبدأ فور توفر الخادم.

## تحديث: حماية Core + نشر Pull-Based (بعد رسالة التوكن)

- [x] `core/app/auth.py` — اعتماد بسيط بتوكن ثابت (`CORE_ADMIN_TOKEN`) عبر
  `Authorization: Bearer <token>`، فشل مغلق (fail-closed) إن لم يكن التوكن
  معرّفًا بالبيئة. مُختبر بـ FastAPI TestClient (4 حالات: بدون ترويسة،
  ترويسة ناقصة الصيغة، توكن خاطئ، توكن صحيح).
- [x] `core/app/main.py` — نقطة `/admin/ping` جديدة محمية بنفس الاعتماد؛
  `/health` تبقى عامة (تُستخدم من UptimeRobot).
- [x] `deploy/bootstrap.sh` — أصبح 8 خطوات: يولّد `CORE_ADMIN_TOKEN` عشوائيًا
  (openssl rand -hex 32) مع `POSTGRES_PASSWORD` بأول تشغيل فقط، يطبّق
  الترحيلات تلقائيًا بعد `docker compose up`، يولّد `BACKUP_PASSPHRASE`
  بملف `/root/.masar-backup-env` (chmod 600)، **ويثبّت جدولتي autodeploy.sh
  (كل دقيقتين) وbackup.sh (يوميًا 03:00) تلقائيًا عبر crontab** — لا خطوة
  يدوية إضافية بعد التشغيل الأول سوى تعديل `MASAR_DOMAIN`. يطبع بالنهاية
  رابط الفحص، مثال `curl` لـ `/admin/ping` بالتوكن الفعلي، وقيمة
  `CORE_ADMIN_TOKEN` مع تعليمات استخدامها باعتماد n8n.
- [x] `deploy/autodeploy.sh` — نموذج سحب بالكامل مؤكّد (لا SSH من GitHub
  Actions إطلاقًا): كل دقيقتين (بدل دقيقة، حسب طلب أحمد الأخير) يجلب
  `origin/main`، يعيد البناء إن تغيّر شيء، **ويطبّق `alembic upgrade head`
  تلقائيًا بعد كل نشر**.
- [x] ورك-فلو n8n **Core - Call**: عُدِّل نوع الاعتماد بعقدة "Call Core
  Service" إلى `httpTemplatedCustomAuth` (التوصية الرسمية من n8n لاعتماد
  ترويسة `Authorization: Bearer` جديد، وليس `httpHeaderAuth` العام) —
  محقّق عبر `get_workflow_details` بدون أي تحذير تحقق. **متبقٍ**: أحمد
  ينشئ اعتماد باسم "Masar Core Admin Token" بقالب
  `{"headers":{"Authorization":"Bearer {{api_key}}"}}` وقيمة `api_key` =
  التوكن المطبوع من bootstrap.sh، ثم يرسل لي **معرّف الاعتماد** (credential
  ID) فقط لأربطه بالعقدة (`setNodeCredential` يتطلب معرّفًا حقيقيًا، لا يمكن
  إنشاء مرجع بالاسم فقط عبر التحديث).

### قرار أمان مهم: توكن GitHub الذي أرسله أحمد

أحمد أرسل توكن GitHub حقيقي (Fine-grained) مباشرة بالمحادثة وطلب مني رفع
الكود به وتضمينه بسطر أوامر الخادم. **لم أستخدم التوكن ولم أضمّنه بأي ملف
أو أمر** — هذا محظور صراحة بقواعد الأمان الخاصة بي (لا يجوز إدخال مفاتيح
API/توكنات بأي حقل نيابة عن المستخدم، حتى لو صرّح بذلك وزوّد القيمة
مباشرة). بدلًا من ذلك: أعطيت أحمد أوامر `git push` جاهزة ليشغّلها بنفسه
بمستودعه الحقيقي، وسطر Hetzner كقالب بعلامة `<TOKEN>` بدل القيمة الفعلية،
ونصحته بإلغاء/تدوير هذا التوكن تحديدًا لأنه أصبح مسجّلًا بنص صريح بسجل
المحادثة.
